# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Model weights & caches — NEVER commit (they live on an external volume, never in iCloud)
*.safetensors
*.gguf
*.ckpt
*.bin
*.pt
/models/
weights/
.hf-cache/

# Generated image outputs
outputs/
runs/
*.png
*.jpg
*.jpeg

# Docs — model & CLI reference docs under docs/ are tracked. Local planning
# notes (design, plans, specs, handoffs, notes) stay on disk only.
docs/design/
docs/plans/
docs/handoff/
docs/notes/
docs/superpowers/
docs/parameters.md

# Local secrets — API keys, never committed
secrets.toml

# OS / editor
.DS_Store
