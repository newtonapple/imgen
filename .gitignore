# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Model weights & caches — NEVER commit (they live on an external volume, never in iCloud)
*.safetensors
*.gguf
*.ckpt
*.bin
*.pt
/models/
weights/
.hf-cache/

# Generated image outputs
outputs/
runs/
*.png
*.jpg
*.jpeg

# Docs — kept on disk, not tracked. The README is the only committed doc;
# everything under docs/ (design notes, specs, plans, parameters) stays local.
docs/

# Local secrets — API keys, never committed
secrets.toml

# OS / editor
.DS_Store
