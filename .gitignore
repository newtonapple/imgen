# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Model weights & caches — NEVER commit (they live on an external volume, never in iCloud)
*.safetensors
*.gguf
*.ckpt
*.bin
*.pt
/models/
weights/
.hf-cache/

# Generated image outputs
outputs/
runs/
*.png
*.jpg
*.jpeg
!docs/**/*.png
!docs/**/*.jpg

# Local planning docs — kept on disk, not tracked (only docs/parameters.md is committed)
docs/design/
docs/notes/
docs/plans/

# Local secrets — API keys, never committed
secrets.toml

# OS / editor
.DS_Store
